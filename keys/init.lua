local keys = {
    globalkeys = require("keys.global.keys"),
    globalbuttons = require("keys.global.buttons"),
    clientkeys = require("keys.client.keys"),
    clientbuttons = require("keys.client.buttons"),
}

return keys
